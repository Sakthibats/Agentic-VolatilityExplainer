# Mirrors [project.dependencies] in pyproject.toml — kept only for the
# devcontainer bootstrap (.devcontainer/devcontainer.json), which installs
# via pip before `pip install -e .` is available. Update both when deps change.
anthropic>=0.40
fastapi>=0.115
httpx>=0.27
numpy>=1.26
pandas>=2.0
pydantic-settings>=2.0
redis>=5.0
sse-starlette>=2.1
supabase>=2.0
uvicorn[standard]>=0.30
yfinance>=1.4,<1.5
